import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.scotelaropitbullteam.app',
  appName: 'Scotelaro Pitbull Team',
  webDir: 'build'
};

export default config;
